name: typescript test

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - run: (cd TypeScript/ && deno fmt --check)
      - run: (cd TypeScript/ && deno lint)
      - run: make testts
